import { HamburgesaDirective } from './hamburgesa.directive';

describe('HamburgesaDirective', () => {
  it('should create an instance', () => {
    const directive = new HamburgesaDirective();
    expect(directive).toBeTruthy();
  });
});
